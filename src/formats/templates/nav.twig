{% if reports|length > 1 %}
   <nav class="nav">
      <ul>
         {% for report in reports %}
            <li>
               <a href="#{{item}}">
                  <strong>{{report.name}}</strong> audit
               </a>
            </li>
         {% endfor %}
      </ul>
   </nav>
{% endif %}
